import { StationPneumatique } from "./stationPneumatique";

describe('StationPneumatique', () => {
    it('should create an instance', () => {
      expect(new StationPneumatique()).toBeTruthy();
    });
  });